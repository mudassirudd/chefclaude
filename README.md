# 🍳 AI Recipe Generator

An AI-powered recipe generator built with **React**.  
Enter your ingredients, and the app returns a Markdown-formatted recipe generated by a **Hugging Face OpenAI-compatible model** (`moonshotai/Kimi-K2-Thinking:novita`).  
Includes optional **Anthropic (Claude)** integration as a fallback.

---

## 🚀 Features

- 🍅 Generate recipes from a user-provided ingredient list (Markdown output)
- ⚙️ Modular React components (`IngredientsList`, `ClaudeRecipe`, `Main.jsx`)
- 🤖 Uses **Hugging Face Router** (OpenAI-compatible) for `moonshotai/Kimi-K2-Thinking:novita`
- 🧠 Optional **Anthropic Claude** integration (if you add an Anthropic API key)
- 🔐 API keys handled securely via **environment variables**

---

## 🧰 Tech Stack

| Category | Tools |
|----------|-------|
| **Frontend** | React (Vite) |
| **AI Integration** | Hugging Face Router (OpenAI-compatible API) |
| **Optional** | Anthropic SDK |
| **Deployment** | Vercel |

---

## ⚙️ Environment Variables

Set these in **Vercel** (**Project → Settings → Environment Variables**)  
and locally in a `.env` file (⚠️ do **not** commit your `.env`):
```bash
HF_ACCESS_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxx

# Optional:
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx
```

---

## 🧑‍💻 Quickstart (Local)
```bash
# Clone repository
git clone https://github.com/yourusername/ai-recipe-generator.git
cd ai-recipe-generator

# Install dependencies
npm install

# Start development server
npm run dev
```

Then open [http://localhost:5173](http://localhost:5173) in your browser.

---

## 🏗️ Build & Deployment (Vercel)

### Steps

1. Push the project to GitHub
2. Go to [https://vercel.com](https://vercel.com)
3. Create a **New Project** → Import your GitHub repo
4. Add your environment variables (`HF_ACCESS_TOKEN`, optional `ANTHROPIC_API_KEY`)
5. Click **Deploy**
6. Copy the deployed URL and add it under "Live Demo"

**Build Settings** (Vite defaults):
- **Build Command:** `npm run build`
- **Output Directory:** `dist`

---

## 🧠 How It Works

1. User adds ingredients in the UI
2. The frontend calls `getRecipeFromMistral()` in `ai.js`
3. `ai.js` sends the request to the Hugging Face router → `https://router.huggingface.co/v1`
4. Model used: `moonshotai/Kimi-K2-Thinking:novita`
5. The model returns Markdown text
6. The Markdown output is displayed in `<ClaudeRecipe>`

---

## 💡 Recommended Additions

- ✅ Render Markdown nicely using `react-markdown`
- 💾 Add LocalStorage caching for ingredients
- 🔁 Add a "Regenerate Recipe" button
- 🧩 Add model-selection dropdown (e.g., choose between Kimi / Claude / Mistral)

---

## 🪪 License

MIT License — free to use, modify, and distribute.

---

**Built by Mudassiruddin Ahmed** 🧠
